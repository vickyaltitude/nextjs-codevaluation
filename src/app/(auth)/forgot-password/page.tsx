export default function ForgotPassword() {
  return <h1>Forgot password</h1>;
}

/* (auth) round bracket tells next js that it is a group of routes and hence it wont nest the auth route so we can access by /login instead of /auth/login */
