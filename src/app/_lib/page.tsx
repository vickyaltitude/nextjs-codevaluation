export default function PrivateFolder() {
  return (
    <h1>
      This is private folder because we prefixed the folder name with
      underscore. So u cannot see this path in browser
    </h1>
  );
}
